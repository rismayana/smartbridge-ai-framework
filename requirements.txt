# SHMS AI Anomaly Detection — Python Dependencies
# Install: pip install -r requirements.txt
# Python versi yang direkomendasikan: 3.10 atau 3.11

# ── Data & numerik ─────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11

# ── Machine learning ───────────────────────────
scikit-learn>=1.3          # Isolation Forest, metrics, preprocessing

# ── Deep learning ──────────────────────────────
torch>=2.0                 # LSTM Autoencoder & GNN (PyTorch)
torch-geometric>=2.4       # Graph Neural Network

# ── Visualisasi ────────────────────────────────
matplotlib>=3.7
seaborn>=0.13

# ── Notebook ───────────────────────────────────
jupyter>=1.0
ipykernel>=6.0

# ── Database (PostgreSQL) ──────────────────────
sqlalchemy>=2.0
psycopg2-binary>=2.9       # ganti dengan psycopg2 jika ada masalah binary

# ── Utilities ──────────────────────────────────
tqdm>=4.65                 # progress bar training
joblib>=1.3                # simpan/load model sklearn
python-dotenv>=1.0         # load .env file untuk credentials

# ── MLOps ──────────────────────────────────────
wandb>=0.17                # experiment tracking & model registry (W&B cloud)
dvc>=3.50                  # data version control
dvc-gdrive>=3.0            # DVC remote storage via Google Drive

# ── Opsional: Google Colab / Drive ─────────────
# google-colab             # sudah tersedia otomatis di Colab
# google-auth>=2.0         # jika perlu akses GDrive dari script lokal
